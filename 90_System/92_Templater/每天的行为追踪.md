<%*
// ================= 自动获取日期 =================
const today = new Date();
const TARGET_DATE = today.toISOString().split('T')[0];
// ===============================================

// 1. 构建内容字符串
// 注意：这里只是定义变量，不会立即写入文件
let content = `# 📅 每日概览：${TARGET_DATE}

---

### ⏳ 时间轴记录
> 这里展示当天的具体时间段记录（TimeLog）

\`\`\`dataviewjs
// 加载外部脚本并渲染时间轴
const s = app.vault.getFileByPath("90_System/94_DataviewJs/TimeViewScript_Func.js");
if (s) {
    try {
        eval(await app.vault.read(s) + "\\nwindow.f=renderTimeline");
        await window.f(dv, "${TARGET_DATE}");
    } catch (e) {
        dv.paragraph("❌ 时间轴脚本加载失败：" + e.message);
    }
} else {
    dv.paragraph("❌ 找不到脚本文件：90_System/94_DataviewJs/TimeViewScript_Func.js");
}
\`\`\`

---

### ✅ 今日任务汇总
> 包含：今天新建、今天截止、今天开始、今天计划、今天完成

\`\`\`tasks
# 逻辑：只要满足以下任意一个条件，就显示出来
( created on ${TARGET_DATE} ) OR ( due on ${TARGET_DATE} ) OR ( starts on ${TARGET_DATE} ) OR ( scheduled on ${TARGET_DATE} ) OR ( done on ${TARGET_DATE} )

# 显示设置
hide backlink
hide task count
\`\`\`

---

### ⚠️ 历史遗留 (过期未完成)
> 截止日期在 **${TARGET_DATE}** 之前，且尚未完成的任务

\`\`\`tasks
due before ${TARGET_DATE}
not done
hide backlink
hide created date
hide start date
hide scheduled date
\`\`\`

---

### 🔥 未来高优任务
> 截止日期在 **${TARGET_DATE}** 之后，但优先级为 **高** 或 **最高** 的任务

\`\`\`tasks
due after ${TARGET_DATE}
not done
priority is high
OR
priority is highest
hide backlink
hide created date
hide start date
hide scheduled date
\`\`\`
`;

tR += content;
-%>